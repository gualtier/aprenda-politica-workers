services:
  redis:
    restart: unless-stopped

  sync-worker:
    restart: unless-stopped

  photo-worker:
    restart: unless-stopped

  news-worker:
    restart: unless-stopped

  scheduler:
    restart: unless-stopped
